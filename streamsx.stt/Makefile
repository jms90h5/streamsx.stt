# Makefile for SPL operators
# This is not actually needed - the root Makefile handles toolkit building

.PHONY: all clean

all:
	@echo "SPL operators are built by the root Makefile"

clean:
	@echo "SPL operators are cleaned by the root Makefile"